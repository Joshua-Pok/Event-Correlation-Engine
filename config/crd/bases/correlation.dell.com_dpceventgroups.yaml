---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: dpceventgroups.correlation.dell.com
spec:
  group: correlation.dell.com
  names:
    kind: DpcEventGroup
    listKind: DpcEventGroupList
    plural: dpceventgroups
    singular: dpceventgroup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DpcEventGroup is the Schema for the dpceventgroups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DpcEventGroup
            properties:
              node:
                type: string
              windowSeconds:
                type: integer
            required:
            - windowSeconds
            type: object
          status:
            description: status defines the observed state of DpcEventGroup
            properties:
              eventCount:
                type: integer
              events:
                items:
                  properties:
                    eventType:
                      type: string
                    name:
                      type: string
                    timestamp:
                      format: date-time
                      type: string
                  required:
                  - eventType
                  - name
                  - timestamp
                  type: object
                type: array
              firstEventTime:
                format: date-time
                type: string
              lastEventTime:
                format: date-time
                type: string
              phase:
                type: string
              windowExpires:
                format: date-time
                type: string
            required:
            - eventCount
            - phase
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
