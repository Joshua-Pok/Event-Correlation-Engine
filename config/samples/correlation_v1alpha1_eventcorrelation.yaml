apiVersion: correlation.dell.com/v1alpha1
kind: EventCorrelation
metadata:
  labels:
    app.kubernetes.io/name: event-correlation-engine
    app.kubernetes.io/managed-by: kustomize
  name: eventcorrelation-sample
spec:
  # TODO(user): Add fields here
